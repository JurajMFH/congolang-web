name: congolang_app
description: Tinder-for-words gamified Lingala and Kituba verification app.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.6
  hive_flutter: ^1.1.0
  flutter_card_swiper: ^0.1.0
  percent_indicator: ^4.2.3
  http: ^1.1.0
  google_fonts: ^5.1.0
  json_annotation: ^4.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
